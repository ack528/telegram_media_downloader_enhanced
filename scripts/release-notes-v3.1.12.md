# v3.1.12

本次更新修复断网时不会自动切换 Clash 美国节点的问题。

## 更新内容

- 新增网络异常触发 Clash 切换：消息抓取失败、下载重试前重新抓取失败、读取群历史失败时，都会通知 Clash 监控器尝试换节点。
- 自动切换不再只依赖“正在下载且速度低于阈值”，断在轮询媒体 ID / 获取消息阶段也能触发。
- 继续复用原有规则模式、美国节点筛选、最低延迟节点选择和切换冷却时间，避免频繁切换。
- 增加回归测试，覆盖“没有活跃下载但发生网络失败时也会切换 Clash”的场景。
